#include "main.h"

/**
 * add -  adds two integers and returns the result.
 *
 * @R:frist input.
 *
 * @M: socsnd input .
 *
 * Return: sum of R + M
*/

int add(int R, int M)
{
	int sum = R + M;

	return (sum);
}
