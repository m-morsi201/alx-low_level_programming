#include "main.h"

/**
* _isupper - func to checks for uppercase character.
* @c: it is a text input.
* Return: 1 if c is uppercase, else (0).
*/

int _isupper(int c)
{


	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
{
	return (0);
}

}
