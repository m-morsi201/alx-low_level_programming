#include "holberton.h"

/**
 *  mul - it is a function to multiplies two integers.
 * @a: it is a first input.
 * @b: it is a second input.
 * Return: multiplication result of the 2 inputs.
*/

int mul(int a, int b)
{
	return (a * b);
}
