#include <stdio.h>

/**
 * main - it is a func to print prog name.
 * @argc: it is int.
 * @argc: it is array.
 * Return: (0).
*/

int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
