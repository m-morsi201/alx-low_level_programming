#include <stdio.h>

/**
 * main - print porg.
 * @argv: it is list.
 * @argc: it is int
 * Return: (0).
*/

int main (int argc, char const *argv[])
{
	(void)argc;
	printf("%s/n", argv[0]);
	return (0);
}
