#include <stdio.h>
#include <stdlib.h>

/**
 * main - it is the entry point.
 * this function will prints the name of the file it was compiled from.
 * Return: (0).
*/

int main(void)
{
	prinf("%s\n", __FILE__);
	return (0);
}
